class MultiplicationTable:
    @classmethod
    def generate(cls):
        for i in range(1, 13):
            for j in range(1, 13):
                print(f"{i} × {j} = {i * j}")

# クラスメソッドを呼び出して掛け算表を生成する
MultiplicationTable.generate()
